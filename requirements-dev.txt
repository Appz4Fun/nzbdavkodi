# Exact pins so local == CI. Bump deliberately; chromadb tooling lives in
# requirements-dev-chroma.txt (separate Python 3.14 env).
pytest==9.1.1
pylint==4.0.6
ruff==0.15.20
black==26.5.1
vermin==1.8.0
